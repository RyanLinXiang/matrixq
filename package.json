{
  "name": "MatrixQ",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "cd ./client && yarn install && cd ../server && yarn install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.17.0"
  }
}